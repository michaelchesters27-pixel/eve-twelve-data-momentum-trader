{
  "name": "eve-twelve-data-momentum-trader",
  "version": "2.4.0",
  "private": true,
  "type": "module",
  "description": "Fixed XAUUSD 8x8 pending-order ladder where Bullet 1 onward earns halfway BE protection, protected stops close one bullet, newest initial-SL failure closes the campaign, and profit/daily-loss controls are selectable from the dashboard.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/server.js",
    "check": "node --check src/server.js && node --check src/features.js && node --check src/twelve-data.js && node --check public/app.js",
    "test": "NODE_ENV=test node --test src/*.test.js"
  }
}
