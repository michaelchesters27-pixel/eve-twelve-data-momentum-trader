{
  "name": "eve-twelve-data-momentum-trader",
  "version": "2.2.0",
  "private": true,
  "type": "module",
  "description": "Fixed XAUUSD 8x8 pending-order ladder with equal 0.01 bullets, halfway BE protection, newest-bullet sentinel, selectable campaign profit target, Twelve Data telemetry and complete campaign replay history.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/server.js",
    "check": "node --check src/server.js && node --check src/features.js && node --check src/twelve-data.js && node --check public/app.js",
    "test": "NODE_ENV=test node --test src/*.test.js"
  }
}
